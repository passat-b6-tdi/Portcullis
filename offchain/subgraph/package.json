{
  "name": "portcullis-subgraph",
  "version": "0.1.0",
  "private": true,
  "description": "The Graph subgraph for the Portcullis settlement firewall",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "build:sepolia": "graph build --network sepolia",
    "build:arc": "graph build --network arc-testnet",
    "deploy:studio": "graph deploy portcullis --network sepolia",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.97.1",
    "@graphprotocol/graph-ts": "0.38.0"
  },
  "devDependencies": {
    "matchstick-as": "0.6.0"
  }
}
